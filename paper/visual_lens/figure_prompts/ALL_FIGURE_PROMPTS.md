# Figure prompts — A Learned Visual Lens

Each file offers three drawing layouts for the same locked scientific content. These are figure alternatives, not different manuscript versions. Layout A corresponds most closely to the included paper. Numerical charts are already reproduced with plotting/make_figures.py; use the prompts to redesign, not to regenerate numbers. The method figure is editable TikZ in main.tex. The final optional figure is pending and is excluded from the paper.

# Figure 1 — The parameter-count / macro-F1 contrast

## Fact lock

MEASURED: Qwen2.5-VL-7B, four BRIGHT events, one support24/query300 pair per event. Frozen mean macro-F1=0.2234. Diagonal lens:64 scalars,0.2794. Full lens:1024 scalars,0.3208. LoRA:10092544 scalars,0.2986. DERIVED:9856x fewer optimized variables for Full vs LoRA, and +2.22 macro-F1 percentage points. MISSING:matched repeated primary results and error bars. The x-axis is not latency, memory, total model parameters, or sample complexity.

## A — Recommended, immediate reading

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Visualize this precise observation: a 1,024-coefficient visual-state controller has higher mean macro-F1 than the 10,092,544-variable LoRA reference in this fixed-budget four-event comparison. The exact points are Diagonal lens (64,27.94), Full visual lens (1024,32.08), and LoRA (10092544,29.86), with macro-F1 expressed as a percentage. Frozen inference is 22.34 percent. The dataset and checkpoint are identical within each event, but optimizer steps and examples processed are not compute-matched. Use a wide, low scatter plot, approximately 3.6:1. Make the horizontal axis logarithmic from about 35 to 30 million, explicitly label the three tick positions 64, 1,024, and 10,092,544, and use a vertical range of 19 to 37 percent. A dashed horizontal reference at 22.34 is labeled Frozen. Use a square for Diagonal, a diamond for Full, and a circle for LoRA. Put labels directly next to the three points, with the numeric F1 underneath each. Give the Full point the strongest visual prominence, not a giant marker. Keep the substantial horizontal separation between 1,024 and 10.1M honest; do not add a smoothed performance frontier through three methods. The main reading path is from the Full point to the far-right LoRA point: more optimized coordinates do not produce a better F1 under this recipe. Reserve at least 75 percent of the area for the data axes. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## B — Alternative reading structure

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Visualize this precise observation: a 1,024-coefficient visual-state controller has higher mean macro-F1 than the 10,092,544-variable LoRA reference in this fixed-budget four-event comparison. The exact points are Diagonal lens (64,27.94), Full visual lens (1024,32.08), and LoRA (10092544,29.86), with macro-F1 expressed as a percentage. Frozen inference is 22.34 percent. The dataset and checkpoint are identical within each event, but optimizer steps and examples processed are not compute-matched. Use two vertically aligned mini-views with a shared method order: Diagonal lens, Full visual lens, LoRA. The upper, dominant 65-percent view is a horizontal dot plot of macro-F1, with its frozen reference at 22.34. The lower view is an independent logarithmic dot strip of optimized-variable counts, not a bar that hides a truncated axis. Align the Full method position across both views. Label both axes explicitly, use the same method shapes in each, and show all three exact values rather than normalizing against an arbitrary best method. This layout lets the reader compare accuracy first, then inspect how little state was optimized. No claim about wall-clock speed or a statistical performance guarantee belongs in this figure. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## C — Alternative evidence organization

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Visualize this precise observation: a 1,024-coefficient visual-state controller has higher mean macro-F1 than the 10,092,544-variable LoRA reference in this fixed-budget four-event comparison. The exact points are Diagonal lens (64,27.94), Full visual lens (1024,32.08), and LoRA (10092544,29.86), with macro-F1 expressed as a percentage. Frozen inference is 22.34 percent. The dataset and checkpoint are identical within each event, but optimizer steps and examples processed are not compute-matched. Use a two-stage comparison composition: the left 60 percent is a paired accuracy display for Full lens 32.08 versus LoRA 29.86 on a clearly scaled percentage axis, with Diagonal 27.94 and Frozen 22.34 retained as smaller reference points. The right 40 percent is a precise typographic parameter accounting: Full 1,024; LoRA 10,092,544; ratio 9,856x. Connect each count to the corresponding plotted method with a thin neutral guide, not a causal arrow. Put a compact footer annotation Optimized variables; basis storage excluded. Do not convert the parameter ratio into a visual area ratio, speed-up, compression of the backbone, or general sample-efficiency theorem. The two views should make one claim: this fixed comparison has a favorable F1-to-learned-state trade-off. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## Caption

A small internal correction competes with a much larger weight update. Qwen2.5-VL-7B, four BRIGHT events, support24/query300, one support set per event. The horizontal axis counts optimized variables; the dashed line is frozen inference. Full-lens macro-F1 exceeds LoRA with 9,856 times fewer learned scalars. Basis storage and compute are accounted for separately.

## Fidelity checklist

- Same empirical scope in all variants.
- Every plotted value matches the fact lock.
- No hidden inference from missing uncertainties.
- No geometric metaphor is presented as a measured manifold.
- The output and input arrows obey the support/query boundary.

---

# Figure 2 — Fitting and reusing the learned visual lens

## Fact lock

CONFIRMED: target labeled support -> answer-loss gradients at selected visual K/V projection outputs -> uncentered second moment -> top-r linear basis -> bounded residual fit with frozen backbone and fixed basis -> reuse across queries. The mask is post-image only for BRIGHT and all image tokens for stitched/single-image tasks. ILLUSTRATIVE: token glyphs and the lens metaphor. NOT PRESENT: an offline historical-domain basis in the primary method, a nonlinear manifold, actual image sharpening, a learned router, synthetic-data generation, or per-query fitting.

## A — Recommended, immediate reading

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Explain the learned visual lens as a support-fitted operator inside a frozen VLM, not an optical image filter. Use the exact computation C_j=sum_i G_ij^T G_ij, B_j=TopEig_r(C_j), A_ej=alpha R_ej/(1+||R_ej||_F), and Z_tilde=Z+M_visual*(ZB)A_eB^T. R starts at zero, and only R is optimized after B is estimated. A query creates its own Z; B and A are shared across queries from the target domain. The main backbone, its source parameter tensors, and the input images do not change. Use two horizontal lanes, roughly a 2.6:1 canvas. The top lane occupies 45 percent of the height and is labeled Fit once on labeled support. Place four boxes left to right: Image + question + correct answer; Visual-state gradients and C; Top-r directions B; Fit bounded A. Use solid arrows for computations and a thin dashed connector to transfer the fitted B,A to the bottom lane. The lower lane is labeled Reuse on unseen queries. Show a visual-state token block Z splitting into an unchanged straight bypass and a small side branch ZB -> A -> B^T. Apply the visual-row mask to the side branch, then sum it back using a plus node before the remaining frozen decoder and candidate answer. Show a small lock on the decoder and tiny filled cells only in A. Emphasize the residual branch without drawing the whole Transformer. In a tiny token strip, use different shapes or outlines for image rows and text rows; only selected image rows receive a direct write. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## B — Alternative reading structure

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Explain the learned visual lens as a support-fitted operator inside a frozen VLM, not an optical image filter. Use the exact computation C_j=sum_i G_ij^T G_ij, B_j=TopEig_r(C_j), A_ej=alpha R_ej/(1+||R_ej||_F), and Z_tilde=Z+M_visual*(ZB)A_eB^T. R starts at zero, and only R is optimized after B is estimated. A query creates its own Z; B and A are shared across queries from the target domain. The main backbone, its source parameter tensors, and the input images do not change. Use a large central residual schematic taking 70 percent of the canvas, with a compact support-calibration panel above. The central schematic is a left-to-right frozen decoder stream and an explicit mathematical magnification of one visual K/V projection. Draw the identity path horizontally; the residual descends into a rectangular low-rank basis B, passes through a small trainable r-by-r matrix A, returns through B^T, and rejoins at plus. A dotted bracket identifies the selected visual rows. The upper support panel contains the three-stage gradient -> second moment -> basis sequence, then a separate arrow from labeled support to coefficient fitting. Do not show query labels entering that panel. Keep B visually locked after extraction, A visually trainable only in the top phase, and both locked for the query phase. Avoid a curved manifold, a camera lens floating over input pixels, or a fake brighter image after editing. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## C — Alternative evidence organization

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Explain the learned visual lens as a support-fitted operator inside a frozen VLM, not an optical image filter. Use the exact computation C_j=sum_i G_ij^T G_ij, B_j=TopEig_r(C_j), A_ej=alpha R_ej/(1+||R_ej||_F), and Z_tilde=Z+M_visual*(ZB)A_eB^T. R starts at zero, and only R is optimized after B is estimated. A query creates its own Z; B and A are shared across queries from the target domain. The main backbone, its source parameter tensors, and the input images do not change. Use a before/after computation comparison with exactly the same frozen model outline and input token order in both rows. The top row is the zero-controller case: A=0 and Z_tilde=Z, with a visible uninterrupted identity route. The lower row is the support-fitted case: the same identity route plus a small masked low-rank branch. Between the rows, place a compact fitting strip showing support answers -> gradients -> B -> A. The output is labeled Candidate scores in both rows, not Wrong and Correct, because no paired example has been selected to establish such an outcome. The comparison should communicate controlled change in computation rather than promise a prediction improvement on every sample. Use a small explicit equation to distinguish an additive residual from hard projection; information orthogonal to B is retained at the intervention site. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## Caption

A lens is an operator, not a replacement model. Correct-answer gradients select a linear visual-state subspace. A bounded matrix is then fitted inside that subspace and reused across query images. The original path remains present. Each query creates its own activations, transformed by the same domain-specific lens.

## Fidelity checklist

- Same empirical scope in all variants.
- Every plotted value matches the fact lock.
- No hidden inference from missing uncertainties.
- No geometric metaphor is presented as a measured manifold.
- The output and input arrows obey the support/query boundary.

---

# Figure 3 — Correctness-sensitive basis controls

## Fact lock

MEASURED: compact Full controller on four BRIGHT events, alpha3. Random rank5:0.2414 averaged over3 basis seeds. Mean gradient rank1:0.2506. Centered rank5:0.2721. Uncentered second moment rank5:0.2964. MISSING:independent pointwise standard errors for the plotted mean bars. These are NOT primary rank16 results. Rank1 mean-gradient is a confounded capacity comparison; centered/random vs second moment are rank matched.

## A — Recommended, immediate reading

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Create a quantitative mechanism comparison for basis construction. Use exactly Random/rank5=24.14, Mean gradient/rank1=25.06, Centered/rank5=27.21, and Second moment/rank5=29.64, in macro-F1 percent. The central comparison is the rank-five second-moment basis against rank-five random and centered alternatives. Preserve the rank-one label on the mean-gradient arm; do not present all four methods as equal-capacity variants. Use one broad bar chart with a zero baseline, vertical extent 0 to 35 percent, and four equal-width bars in the stated order. Put the rank on a second line of each x label. Print each exact F1 above the bar. Use one consistent neutral or muted palette with a stronger outline on the second-moment bar; avoid color as the only method code. Add a thin bracket under the Random, Centered, and Second moment names if useful to mark Rank-matched, but do not connect non-adjacent bars through a confusing line. A small note below the plot records Random: three basis seeds; others: one support configuration. The figure shows observed means and no significance stars or invented intervals. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## B — Alternative reading structure

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Create a quantitative mechanism comparison for basis construction. Use exactly Random/rank5=24.14, Mean gradient/rank1=25.06, Centered/rank5=27.21, and Second moment/rank5=29.64, in macro-F1 percent. The central comparison is the rank-five second-moment basis against rank-five random and centered alternatives. Preserve the rank-one label on the mean-gradient arm; do not present all four methods as equal-capacity variants. Use a horizontal point-comparison chart rather than bars, with one row per basis. The x-axis is macro-F1 percentage, clearly spanning 20 to 33. Put rank5/rank1 in a dedicated text column next to each row. Align all numerical scores in a right-side value column. A faint vertical reference at the second-moment score 29.64 allows the deficit of each control to be read directly, without implying every individual random seed is worse. Keep the mean-gradient row separated by a small gap or a footnote mark identifying its different rank. This layout emphasizes the specific ablations instead of visually suggesting an algorithmic progression from one method to the next. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## C — Alternative evidence organization

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Create a quantitative mechanism comparison for basis construction. Use exactly Random/rank5=24.14, Mean gradient/rank1=25.06, Centered/rank5=27.21, and Second moment/rank5=29.64, in macro-F1 percent. The central comparison is the rank-five second-moment basis against rank-five random and centered alternatives. Preserve the rank-one label on the mean-gradient arm; do not present all four methods as equal-capacity variants. Use a compact matrix-style evidence display: three rank-five rows, Random, Centered, Second moment, form the dominant upper block; a rank-one mean-gradient result forms a clearly separate lower control block. In the upper block, each row shows a tiny equal-size abstract basis glyph followed by a precise macro-F1 dot on a shared axis. The glyphs are conceptual and must not look like actual learned singular vectors or saliency maps. Label the measured score next to the dot, retain the 3-seed random annotation, and keep all mean-gradient information in the lower block. The scientific message is that the data-defined uncentered directions improve this compact controller, while the distinct rank-one comparison needs separate interpretation. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## Caption

The choice of visual subspace matters. Four-event BRIGHT mean macro-F1, compact full lens, alpha=3. Random, centered, and second-moment bases have rank5; mean-gradient has rank1. Random averages three basis seeds; the other constructions use one support configuration.

## Fidelity checklist

- Same empirical scope in all variants.
- Every plotted value matches the fact lock.
- No hidden inference from missing uncertainties.
- No geometric metaphor is presented as a measured manifold.
- The output and input arrows obey the support/query boundary.

---

# Figure 4 — Prior mixture changes the diagnostic direction

## Fact lock

MEASURED ORACLE STATISTIC: RoboFail, InternVL3, QKVO projections, rank16. Seed0 kappa -0.2065 ->0.9070; seed1 0.3866 ->0.9375; seed2 0.4475 ->0.9574. Original mean±sampleSD0.2092±0.3613; oracle reweighted0.9340±0.0254. Fixed aggregate support basis. Query-label prior is used only to reweight support class-gradient means. NOT MEASURED: a reweighted deployment controller or an improved F1. Aggregate kappa is query-energy-weighted module cosine, not a cosine of a globally concatenated gradient.

## A — Recommended, immediate reading

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Visualize an oracle diagnostic of directional mixing, not a new adaptation method. Two conditions use the same support-derived basis: Original support mixture and Query-prior mixture (oracle). Plot seed0 -0.2065 to0.9070, seed1 0.3866 to0.9375, and seed2 0.4475 to0.9574. The exact aggregate statistic is the query-gradient-energy-weighted average of module-level projected mean-gradient cosines. The changed prior is obtained using query labels, and no new classifier score is implied. Use a two-condition paired slope chart. Horizontal positions are only the two conditions; the y-axis runs from -1 to1.1 and is labeled Directional agreement kappa. Connect the same seed across conditions with a thin line. Encode the three seeds with circle, square, and diamond markers as well as distinguishable default colors. Show a light zero reference and a small legend in the lower-right empty region. Do not fit a trend line across datasets or turn the three points into a population distribution. Keep a visible Oracle label in the second condition. The shape of the result should be obvious: seed0 reverses sign, and all three reweighted values concentrate near positive agreement. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## B — Alternative reading structure

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Visualize an oracle diagnostic of directional mixing, not a new adaptation method. Two conditions use the same support-derived basis: Original support mixture and Query-prior mixture (oracle). Plot seed0 -0.2065 to0.9070, seed1 0.3866 to0.9375, and seed2 0.4475 to0.9574. The exact aggregate statistic is the query-gradient-energy-weighted average of module-level projected mean-gradient cosines. The changed prior is obtained using query labels, and no new classifier score is implied. Use three horizontal dumbbell rows, one per support seed. The shared horizontal axis spans -1 to1 and is labeled kappa. Original and oracle-reweighted conditions use hollow and filled versions of the same marker, connected within the row. Print exact endpoints to four decimal places in small, well-aligned labels. A compact header above the plot states Fixed support basis. Put the word Oracle next to the filled-marker legend. This composition gives equal attention to each paired observation and prevents the large seed0 shift from hiding the smaller but still real changes in seeds1 and2. Do not add any success/failure prediction examples or imply that the F1 gap to LoRA closed. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## C — Alternative evidence organization

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Visualize an oracle diagnostic of directional mixing, not a new adaptation method. Two conditions use the same support-derived basis: Original support mixture and Query-prior mixture (oracle). Plot seed0 -0.2065 to0.9070, seed1 0.3866 to0.9375, and seed2 0.4475 to0.9574. The exact aggregate statistic is the query-gradient-energy-weighted average of module-level projected mean-gradient cosines. The changed prior is obtained using query labels, and no new classifier score is implied. Use a measured-plus-conceptual composition with a strict boundary: left 65 percent contains the exact paired kappa plot; right 35 percent contains a clearly labeled Conceptual mixture diagram. In the conceptual panel draw two class-gradient arrows and their weighted sums, illustrating that class-wise directions may remain similar while the aggregate changes with mixture weights. Do not assign arrow coordinates or lengths as measured values. Use the observed seed0 class counts only as a text annotation, Support8/8 and Query116/21, without claiming they describe every seed. Separate this illustration from the measured axis with a dotted rule. Keep the oracle qualifier in both panels and do not replace the measured plot with arbitrary geometric arrows. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## Caption

Class mixing can reverse an aggregate direction. RoboFail oracle diagnostic, three support seeds, InternVL3. Reweighting support class means with the query class prior raises the energy-weighted alignment from0.2092±0.3613 to0.9340±0.0254 with the same aggregate support basis. This changes a diagnostic statistic, not the fitted controller or its F1.

## Fidelity checklist

- Same empirical scope in all variants.
- Every plotted value matches the fact lock.
- No hidden inference from missing uncertainties.
- No geometric metaphor is presented as a measured manifold.
- The output and input arrows obey the support/query boundary.

---

# Appendix Figure 5 — Module-wise directional agreement

## Fact lock

MEASURED: three-support-seed means, InternVL3. Q,K,V,O in order: Camelyon17 [.9801,.9676,.9905,.9904]; RoboFail [.425,.438,.034,-.120]; ManipBench DROID-pick [.863,.626,.838,.881]. Query labels are used. MISSING:module-level SDs and raw per-seed values for every cell. These are distinct from the aggregate rho or aggregate kappa.

## A — Recommended, immediate reading

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Show the exact three-by-four module-level directional-agreement matrix. Rows are Camelyon17, RoboFail, and ManipBench DROID-pick. Columns are Q,K,V,O. Values respectively are [0.9801,0.9676,0.9905,0.9904], [0.425,0.438,0.034,-0.120], and [0.863,0.626,0.838,0.881]. Each value is a three-support-seed mean of a query-label mechanism diagnostic. The scientific message is module heterogeneity, particularly the nearly zero V and negative O mean on RoboFail. Use a wide annotated heatmap, about 2.7:1, with a continuous colorbar fixed to -1 through1. Put a signed three-decimal value in every cell, using a neutral text backing or contrast-safe label. Do not hide the minus sign on -0.120. Show short task labels on the left and module letters on top. The colorbar label is Mean kappa. Use thin or absent cell boundaries, and rely on explicit values to support grayscale readability. A small footer states Query-label diagnostic; means over3 support seeds. Do not put performance arrows or F1 scores in these cells. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## B — Alternative reading structure

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Show the exact three-by-four module-level directional-agreement matrix. Rows are Camelyon17, RoboFail, and ManipBench DROID-pick. Columns are Q,K,V,O. Values respectively are [0.9801,0.9676,0.9905,0.9904], [0.425,0.438,0.034,-0.120], and [0.863,0.626,0.838,0.881]. Each value is a three-support-seed mean of a query-label mechanism diagnostic. The scientific message is module heterogeneity, particularly the nearly zero V and negative O mean on RoboFail. Use three stacked horizontal module-profile charts, generated as separate panels sharing the same -1-to1 scale. Within each panel place four labeled points for Q,K,V,O and connect them only as a visual guide through the categorical module order, not a temporal process. Mark zero with a thin reference. Avoid smoothing. Use one marker shape per task, and keep task names next to the panels so no legend lookup is required. This layout emphasizes how RoboFail differs from the other tasks without changing its axis range or exaggerating the weak V value. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## C — Alternative evidence organization

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Show the exact three-by-four module-level directional-agreement matrix. Rows are Camelyon17, RoboFail, and ManipBench DROID-pick. Columns are Q,K,V,O. Values respectively are [0.9801,0.9676,0.9905,0.9904], [0.425,0.438,0.034,-0.120], and [0.863,0.626,0.838,0.881]. Each value is a three-support-seed mean of a query-label mechanism diagnostic. The scientific message is module heterogeneity, particularly the nearly zero V and negative O mean on RoboFail. Use a compact signed dot matrix rather than a heatmap: rows are datasets, columns are modules, and each cell contains a tiny horizontal -1-to1 number line with a point at its true value plus the signed numeric annotation. All micro-axes must have identical limits. Use an explicit zero tick in every cell. Keep the plot large enough to remain legible or use it only as an appendix figure, since12 micro-axes need adequate space. Do not make dot area proportional to F1 or to an unsupported confidence estimate. The matrix keeps the measured object, module-level kappa, explicit even without color. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## Caption

Directional agreement depends on the projection. Three-support-seed means of the query-label diagnostic. Q/K/V/O are analyzed separately; the plotted entries are not the aggregate cosine. Negative and positive values are annotated explicitly.

## Fidelity checklist

- Same empirical scope in all variants.
- Every plotted value matches the fact lock.
- No hidden inference from missing uncertainties.
- No geometric metaphor is presented as a measured manifold.
- The output and input arrows obey the support/query boundary.

---

# Optional future figure — Does the lens change visual evidence use?

## Fact lock

MISSING EXPERIMENT. No verified per-image saliency, attention, image-ablation, or same-sample counterfactual prediction artifacts were supplied. Do not put this optional figure into the current manuscript as completed evidence. Populate only after the declared image-control experiment. The current manuscript uses the accurate method schematic instead.

## A — Recommended, immediate reading

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Design an unpopulated layout specification for a future visual-evidence experiment. Do not generate numerical results, fake satellite/pathology images, heatmaps, model outputs, or a wrong-to-correct example. The intended experiment compares the same saved model and fitted controller under genuine images, a deterministic image-shuffle control, and a declared image-removal or occlusion control. A frozen and a lens path must receive identical variants. After the experiment, load real image crops and saved candidate scores, preserving IDs and the predetermined example-selection rule. Plan a two-row filmstrip: Frozen and Visual lens. Columns are the original image, the declared control image, the candidate-score vector, and the model decision. Every crop and prediction must come from the same identified example and recorded intervention. Reserve an empty evidence panel with the explicit label Awaiting experiment in the author-side mockup. Do not show that placeholder in a submission PDF. Choose example IDs before inspecting which cases make the method look best, or sample from all four outcome categories: both correct, lens-only correct, frozen-only correct, both wrong. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## B — Alternative reading structure

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Design an unpopulated layout specification for a future visual-evidence experiment. Do not generate numerical results, fake satellite/pathology images, heatmaps, model outputs, or a wrong-to-correct example. The intended experiment compares the same saved model and fitted controller under genuine images, a deterministic image-shuffle control, and a declared image-removal or occlusion control. A frozen and a lens path must receive identical variants. After the experiment, load real image crops and saved candidate scores, preserving IDs and the predetermined example-selection rule. Plan one quantitative panel comparing lens-minus-frozen effects under intact, shuffled, and removed visual inputs. The metric and target population must be fixed before evaluation. Use paired group-level confidence intervals only after raw predictions exist. Reserve a separate small qualitative panel for two measured examples. The contrast should test whether the advantage depends on the actual image evidence, rather than merely changing answer priors. Never relabel a blank-image intervention as capability removal, since it changes the input distribution and may have its own effects. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## C — Alternative evidence organization

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Design an unpopulated layout specification for a future visual-evidence experiment. Do not generate numerical results, fake satellite/pathology images, heatmaps, model outputs, or a wrong-to-correct example. The intended experiment compares the same saved model and fitted controller under genuine images, a deterministic image-shuffle control, and a declared image-removal or occlusion control. A frozen and a lens path must receive identical variants. After the experiment, load real image crops and saved candidate scores, preserving IDs and the predetermined example-selection rule. Plan a causal-audit schematic on the left showing fixed weights, fixed text, and fixed learned controller with only image content varied. On the right, reserve the yet-unmeasured effect plot. A dashed boundary marks what changes and what stays fixed. Do not assert attention refocusing from an unobserved heatmap. If attention is later visualized, keep it a descriptive internal statistic and add separate prediction interventions; attention brightness alone is not the causal outcome. This is a future evidence design, not an extra result supporting the present draft. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## Caption

Caption to be finalized only after the image-control experiment, with actual sample count, intervention definition, paired estimates, and uncertainty procedure.

## Fidelity checklist

- Same empirical scope in all variants.
- Every plotted value matches the fact lock.
- No hidden inference from missing uncertainties.
- No geometric metaphor is presented as a measured manifold.
- The output and input arrows obey the support/query boundary.

---
