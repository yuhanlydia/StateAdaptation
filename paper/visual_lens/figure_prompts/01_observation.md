# Figure 1 — The parameter-count / macro-F1 contrast

## Fact lock

MEASURED: Qwen2.5-VL-7B, four BRIGHT events, one support24/query300 pair per event. Frozen mean macro-F1=0.2234. Diagonal lens:64 scalars,0.2794. Full lens:1024 scalars,0.3208. LoRA:10092544 scalars,0.2986. DERIVED:9856x fewer optimized variables for Full vs LoRA, and +2.22 macro-F1 percentage points. MISSING:matched repeated primary results and error bars. The x-axis is not latency, memory, total model parameters, or sample complexity.

## A — Recommended, immediate reading

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Visualize this precise observation: a 1,024-coefficient visual-state controller has higher mean macro-F1 than the 10,092,544-variable LoRA reference in this fixed-budget four-event comparison. The exact points are Diagonal lens (64,27.94), Full visual lens (1024,32.08), and LoRA (10092544,29.86), with macro-F1 expressed as a percentage. Frozen inference is 22.34 percent. The dataset and checkpoint are identical within each event, but optimizer steps and examples processed are not compute-matched. Use a wide, low scatter plot, approximately 3.6:1. Make the horizontal axis logarithmic from about 35 to 30 million, explicitly label the three tick positions 64, 1,024, and 10,092,544, and use a vertical range of 19 to 37 percent. A dashed horizontal reference at 22.34 is labeled Frozen. Use a square for Diagonal, a diamond for Full, and a circle for LoRA. Put labels directly next to the three points, with the numeric F1 underneath each. Give the Full point the strongest visual prominence, not a giant marker. Keep the substantial horizontal separation between 1,024 and 10.1M honest; do not add a smoothed performance frontier through three methods. The main reading path is from the Full point to the far-right LoRA point: more optimized coordinates do not produce a better F1 under this recipe. Reserve at least 75 percent of the area for the data axes. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## B — Alternative reading structure

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Visualize this precise observation: a 1,024-coefficient visual-state controller has higher mean macro-F1 than the 10,092,544-variable LoRA reference in this fixed-budget four-event comparison. The exact points are Diagonal lens (64,27.94), Full visual lens (1024,32.08), and LoRA (10092544,29.86), with macro-F1 expressed as a percentage. Frozen inference is 22.34 percent. The dataset and checkpoint are identical within each event, but optimizer steps and examples processed are not compute-matched. Use two vertically aligned mini-views with a shared method order: Diagonal lens, Full visual lens, LoRA. The upper, dominant 65-percent view is a horizontal dot plot of macro-F1, with its frozen reference at 22.34. The lower view is an independent logarithmic dot strip of optimized-variable counts, not a bar that hides a truncated axis. Align the Full method position across both views. Label both axes explicitly, use the same method shapes in each, and show all three exact values rather than normalizing against an arbitrary best method. This layout lets the reader compare accuracy first, then inspect how little state was optimized. No claim about wall-clock speed or a statistical performance guarantee belongs in this figure. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## C — Alternative evidence organization

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Visualize this precise observation: a 1,024-coefficient visual-state controller has higher mean macro-F1 than the 10,092,544-variable LoRA reference in this fixed-budget four-event comparison. The exact points are Diagonal lens (64,27.94), Full visual lens (1024,32.08), and LoRA (10092544,29.86), with macro-F1 expressed as a percentage. Frozen inference is 22.34 percent. The dataset and checkpoint are identical within each event, but optimizer steps and examples processed are not compute-matched. Use a two-stage comparison composition: the left 60 percent is a paired accuracy display for Full lens 32.08 versus LoRA 29.86 on a clearly scaled percentage axis, with Diagonal 27.94 and Frozen 22.34 retained as smaller reference points. The right 40 percent is a precise typographic parameter accounting: Full 1,024; LoRA 10,092,544; ratio 9,856x. Connect each count to the corresponding plotted method with a thin neutral guide, not a causal arrow. Put a compact footer annotation Optimized variables; basis storage excluded. Do not convert the parameter ratio into a visual area ratio, speed-up, compression of the backbone, or general sample-efficiency theorem. The two views should make one claim: this fixed comparison has a favorable F1-to-learned-state trade-off. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## Caption

A small internal correction competes with a much larger weight update. Qwen2.5-VL-7B, four BRIGHT events, support24/query300, one support set per event. The horizontal axis counts optimized variables; the dashed line is frozen inference. Full-lens macro-F1 exceeds LoRA with 9,856 times fewer learned scalars. Basis storage and compute are accounted for separately.

## Fidelity checklist

- Same empirical scope in all variants.
- Every plotted value matches the fact lock.
- No hidden inference from missing uncertainties.
- No geometric metaphor is presented as a measured manifold.
- The output and input arrows obey the support/query boundary.
