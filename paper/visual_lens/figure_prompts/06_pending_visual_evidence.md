# Optional future figure — Does the lens change visual evidence use?

## Fact lock

MISSING EXPERIMENT. No verified per-image saliency, attention, image-ablation, or same-sample counterfactual prediction artifacts were supplied. Do not put this optional figure into the current manuscript as completed evidence. Populate only after the declared image-control experiment. The current manuscript uses the accurate method schematic instead.

## A — Recommended, immediate reading

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Design an unpopulated layout specification for a future visual-evidence experiment. Do not generate numerical results, fake satellite/pathology images, heatmaps, model outputs, or a wrong-to-correct example. The intended experiment compares the same saved model and fitted controller under genuine images, a deterministic image-shuffle control, and a declared image-removal or occlusion control. A frozen and a lens path must receive identical variants. After the experiment, load real image crops and saved candidate scores, preserving IDs and the predetermined example-selection rule. Plan a two-row filmstrip: Frozen and Visual lens. Columns are the original image, the declared control image, the candidate-score vector, and the model decision. Every crop and prediction must come from the same identified example and recorded intervention. Reserve an empty evidence panel with the explicit label Awaiting experiment in the author-side mockup. Do not show that placeholder in a submission PDF. Choose example IDs before inspecting which cases make the method look best, or sample from all four outcome categories: both correct, lens-only correct, frozen-only correct, both wrong. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## B — Alternative reading structure

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Design an unpopulated layout specification for a future visual-evidence experiment. Do not generate numerical results, fake satellite/pathology images, heatmaps, model outputs, or a wrong-to-correct example. The intended experiment compares the same saved model and fitted controller under genuine images, a deterministic image-shuffle control, and a declared image-removal or occlusion control. A frozen and a lens path must receive identical variants. After the experiment, load real image crops and saved candidate scores, preserving IDs and the predetermined example-selection rule. Plan one quantitative panel comparing lens-minus-frozen effects under intact, shuffled, and removed visual inputs. The metric and target population must be fixed before evaluation. Use paired group-level confidence intervals only after raw predictions exist. Reserve a separate small qualitative panel for two measured examples. The contrast should test whether the advantage depends on the actual image evidence, rather than merely changing answer priors. Never relabel a blank-image intervention as capability removal, since it changes the input distribution and may have its own effects. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## C — Alternative evidence organization

```text
Use a clean publication figure on white, with a single readable visual argument, no decorative title banner, no author names, no conference logos, and no dashboard styling. Design for a 5.5-inch-wide single-column ICLR page. Use legible sans-serif labels that remain at least 8 points at final print size, restrained line weights, generous spacing, and shape or line-style encoding in addition to any color. Do not shrink a paragraph into a figure. Keep mathematical notation exact. The supplied values and their scope are immutable. Do not invent uncertainty intervals, significance stars, sample-level images, regression fits, attention maps, or new measurements. The figure must remain interpretable in grayscale. Return vector artwork or an editable layout; numerical coordinates must be plotted programmatically rather than approximated by an image model. Design an unpopulated layout specification for a future visual-evidence experiment. Do not generate numerical results, fake satellite/pathology images, heatmaps, model outputs, or a wrong-to-correct example. The intended experiment compares the same saved model and fitted controller under genuine images, a deterministic image-shuffle control, and a declared image-removal or occlusion control. A frozen and a lens path must receive identical variants. After the experiment, load real image crops and saved candidate scores, preserving IDs and the predetermined example-selection rule. Plan a causal-audit schematic on the left showing fixed weights, fixed text, and fixed learned controller with only image content varied. On the right, reserve the yet-unmeasured effect plot. A dashed boundary marks what changes and what stays fixed. Do not assert attention refocusing from an unobserved heatmap. If attention is later visualized, keep it a descriptive internal statistic and add separate prediction interventions; attention brightness alone is not the causal outcome. This is a future evidence design, not an extra result supporting the present draft. Before finalizing, verify every label, number, arrow, and qualifier against the fact lock. The desired output is one uncluttered visual argument, not a full paper compressed into an image.
```

## Caption

Caption to be finalized only after the image-control experiment, with actual sample count, intervention definition, paired estimates, and uncertainty procedure.

## Fidelity checklist

- Same empirical scope in all variants.
- Every plotted value matches the fact lock.
- No hidden inference from missing uncertainties.
- No geometric metaphor is presented as a measured manifold.
- The output and input arrows obey the support/query boundary.
